<Window x:Class="Glitter.Graph.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:graphsharp="clr-namespace:GraphSharp.Controls;assembly=GraphSharp.Controls"
        xmlns:zoom="clr-namespace:WPFExtensions.Controls;assembly=WPFExtensions" 
        xmlns:local="clr-namespace:Glitter.Graph"
        Title="MainWindow" Height="350" Width="525" WindowState="Maximized"
        AllowDrop="True" DragEnter="allowdrop_DragEnter" Drop="allowdrop_Drop" >
    <Window.Resources>
        <local:ColorConverter x:Key="converter" />
        <DataTemplate x:Key="demoTemplate" DataType="{x:Type local:FileVertex}">
            <TextBlock Text="{Binding Path=Id, Mode=OneWay}" Foreground="White"  />
        </DataTemplate>

        <Style TargetType="{x:Type graphsharp:VertexControl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type graphsharp:VertexControl}">
                        <Border BorderBrush="White" 
                                    Background="{Binding Path=ObjectType, Converter={StaticResource converter } }"
							        BorderThickness="2"
							        CornerRadius="10,10,10,10"
							        Padding="5">

                            <ContentPresenter Content="{TemplateBinding Vertex}" 
                                            ContentTemplate="{StaticResource demoTemplate}">
                                <ContentPresenter.ToolTip>
                                    <TextBlock Text="{Binding Path=Body}" />
                                </ContentPresenter.ToolTip>
                            </ContentPresenter>
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="2" Color="LightGray" 
                                    Opacity="0.3" Direction="315"/>
                            </Border.Effect>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style TargetType="{x:Type graphsharp:EdgeControl}">
            <Style.Resources>
                <ToolTip x:Key="ToolTipContent">
                    <StackPanel>
                        <TextBlock FontWeight="Bold" Text="Edge Information"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                </ToolTip>
            </Style.Resources>
            <Setter Property="ToolTip" Value="{StaticResource ToolTipContent}"/>
        </Style>
    </Window.Resources>
    <Grid>
        <TextBlock HorizontalAlignment="Center" 
                   TextWrapping="Wrap" Text="Drop a repository" 
                   VerticalAlignment="Center"
                   FontSize="36"
                   Name="Banner" Margin="119,134,119,137"/>
        <zoom:ZoomControl  ZoomBoxOpacity="0.5" MouseDoubleClick="zoom_MouseDoubleClick"
                           Background="#ff656565" 
                           Margin="0,-6,0,6" 
                           x:Name="zoom"
                           HorizontalAlignment="Stretch"
                           VerticalAlignment="Stretch"
                           Visibility="Hidden">
            <local:FileGraphLayout x:Name="graphLayout" Margin="10"
                                   Graph="{Binding Path=Graph}"
                                   LayoutAlgorithmType="FR" 
                                   OverlapRemovalAlgorithmType="FSA"
                                   HighlightAlgorithmType="Simple" />
        </zoom:ZoomControl>
    </Grid>
</Window>
