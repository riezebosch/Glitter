<Window x:Name="allowdrop" x:Class="Glitter.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" AllowDrop="True" DragEnter="allowdrop_DragEnter" Drop="allowdrop_Drop">
    <Grid>
        <TextBlock HorizontalAlignment="Center" 
                   TextWrapping="Wrap" Text="Drop a repository" 
                   VerticalAlignment="Center"
                   FontSize="36"
                   Name="Banner" Margin="119,134,119,137"/>
        <ScrollViewer Name="SV">
            <StackPanel HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Name="SP" CanVerticallyScroll="True"
                    />
        </ScrollViewer>
    </Grid>
</Window>
